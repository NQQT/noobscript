// Widget Prop Type takes data as reference
export type WidgetPropsType = {
    // The alias (if needed)
    alias?: {};
    // The original reference data
    data: {
        [key: string]: any;
    };
};
