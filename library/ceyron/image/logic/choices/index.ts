/**
 * Choices allowed the player to choose a number of available options.
 * */

export const choices = (id: string) => {
  // Choices will accept string name only. From there, it will read the data
  return {};
};
