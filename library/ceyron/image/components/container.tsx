import React from 'react';

/**
 * This is a container of all the natural nodes.
 * It should allow interactions and what is not. It might also display necessary information.
 */

export const Container = React.memo(() => {
  return <div></div>;
});
