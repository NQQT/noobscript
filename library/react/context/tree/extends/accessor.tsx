/**
 * Additional Functionality to Tree Context
 * This will convert accessor function to object type
 * It will take care of chaining up and down
 *
 */

export const accessorObject = (accessor: any) => {
  // You must insert accessor function
  // Both reactive and accessor function will work
};
