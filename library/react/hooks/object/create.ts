/** useObject hook is great for data manipulation */
export const createReactObjectHook = (react: any) => {
  // The Initialiser Function
  return (initialObject: { [key: string]: any } = {}) => {
    // A Work in progress
    return null;
  };
};
