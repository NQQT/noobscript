import { storybookTestRunner } from '@library/test';
import * as stories from './AgentComponentChip.stories';

describe('React StateHook Interaction Test', () => {
    storybookTestRunner(stories);
});
